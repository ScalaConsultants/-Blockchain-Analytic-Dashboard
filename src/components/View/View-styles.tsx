import { makeStyles } from '@material-ui/core/styles';
import { ExtendedTheme } from '../../types';

export const useViewStyles = makeStyles((theme: ExtendedTheme) => ({
  root: {
    margin: '100px 0 0 0'
  }
}))